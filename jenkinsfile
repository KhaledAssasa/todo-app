node {

    checkout scm

    docker.withRegistry('https://registry.hub.docker.com', 'dockerHub') {

        def customImage = docker.build("135331/todo")

        /* Push the container to the custom Registry */
        customImage.push()
    }
}
